/* import { Meta } from '@storybook/react';
import React from 'react';
import FormSubmit from '../components/Form';

const LogInfo = (props: string) => {
  console.log(props);
};

const meta: Meta = {
  title: 'MyForm',
  component: FormSubmit,
};

export default meta;

export function Form() {
  return <FormSubmit onFormSubmit={LogInfo} />;
}
*/
export {};
